Remove the "Maybe later" button and the 7-day dismiss logic from `src/components/install-app-card.tsx`. The card shows a single **Install app** button. The card still auto-hides once the app is actually installed (display-mode: standalone). No other changes.