In `src/routes/_authenticated/admin/upload.tsx`, in the current guest list row (around lines 1474–1497), wrap the "RSVP for them" `<Select>` in `{!g.rsvp_status && ( ... )}` so it only renders for guests still awaiting a response. Once a guest is yes / no / waitlist, the dropdown disappears and only the status pill remains. Remove button and "I sent the text" checkbox stay as-is. UI-only, one file, no logic changes.