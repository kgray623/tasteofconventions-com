Update Kenda Andersen's inviter row: set `quota = 0` so she has no seats allocated until she requests a quota through the committee workspace.

No code changes — just a data update on the `inviters` table for Kenda.